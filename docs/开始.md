# 1. 打开终端1 - 激活环境
source /opt/ros/humble/setup.bash
echo $ROS_DISTRO  # 应该显示 "humble"

# 2. 运行快速检查
ros2 --help  # 应该显示帮助信息，没有错误

# 在终端1继续（或者新开终端1）
source /opt/ros/humble/setup.bash
cd ~/qingfu_moveit

# 启动Panda演示
ros2 launch moveit_resources_panda_moveit_config demo.launch.py

# 等待直到看到类似这样的输出：
# [INFO] [launch]: All log files can be found below /home/xxx/.ros/log/...
# [INFO] [move_group-1]: process started with pid [12345]

# 打开终端2 - 新终端也需要激活环境
source /opt/ros/humble/setup.bash

# 快速检查话题
ros2 topic list | head -10  # 应该看到很多话题
# 在终端2继续
cd ~/qingfu_moveit

# 运行最简单的测试
python3 scripts/basic_control.py

# 观察：
# 1. 终端2应该显示"检查连接... ✅ 连接成功"
# 2. 终端1的RViz窗口中，机械臂应该开始运动
# 3. 终端2显示"Home位置到达成功"等消息