# 添加一个物体在位置1
python3 ps-add-object --box --name "train_pos1" --position "0.4,0.0,0.3" --orientation "0,0,0,1" --yes

# 添加一个物体在位置2  
python3 ps-add-object --box --name "train_pos2" --position "0.5,0.1,0.3" --orientation "0,0,0,1" --yes

# 添加一个物体在位置3
python3 ps-add-object --box --name "train_pos3" --position "0.6,0.0,0.4" --orientation "0,0,0,1" --yes

# 添加一个物体在位置4
python3 ps-add-object --box --name "train_pos4" --position "0.45,-0.1,0.35" --orientation "0,0,0,1" --yes

# 添加一个物体在位置5
python3 ps-add-object --box --name "train_pos5" --position "0.55,0.15,0.45" --orientation "0,0,0,1" --yes

cd ~/qingfu_moveit/moveit_ros/planning_interface/move_group_interface/scripts/
python3 ros-move-group-grasp grasp --object train_pos1
python3 ros-move-group-grasp grasp --object train_pos2
python3 ros-move-group-grasp grasp --object train_pos3
python3 ros-move-group-grasp grasp --object train_pos4
python3 ros-move-group-grasp grasp --object train_pos5


给我一个完整的训练流程
📋 完整的ML训练流程
步骤1：清理旧数据（可选）
bash
# 清理IK缓存
rm ~/qingfu_moveit/moveit_core/cache_manager/data/kinematics/ik_solutions/ik_*.json 2>/dev/null

# 清理ML数据（重新开始）
rm -rf ~/qingfu_moveit/moveit_plugins/kinematics_plugins/ml_seed_predictor/data/*
mkdir -p ~/qingfu_moveit/moveit_plugins/kinematics_plugins/ml_seed_predictor/data
步骤2：添加训练物体（5个不同位置）
bash
cd ~/qingfu_moveit/moveit_core/planning_scene/collision_objects/scripts/

# 添加5个安全位置的物体
python3 ps-add-object --box --name "train_1" --position "0.4,0.0,0.3" --orientation "0,0,0,1" --yes
python3 ps-add-object --box --name "train_2" --position "0.45,0.1,0.35" --orientation "0,0,0,1" --yes
python3 ps-add-object --box --name "train_3" --position "0.5,0.0,0.4" --orientation "0,0,0,1" --yes
python3 ps-add-object --box --name "train_4" --position "0.55,-0.1,0.45" --orientation "0,0,0,1" --yes
python3 ps-add-object --box --name "train_5" --position "0.6,0.0,0.5" --orientation "0,0,0,1" --yes
步骤3：循环抓取-释放收集数据
bash
cd ~/qingfu_moveit/moveit_ros/planning_interface/move_group_interface/scripts/
./train_loop.sh
# 创建训练脚本
cat > train_loop.sh << 'EOF'
#!/bin/bash
for i in {1..3}; do
    echo "===== 第 $i 轮训练 ====="
    
    for obj in train_1 train_2 train_3 train_4 train_5; do
        echo "抓取 $obj..."
        python3 ros-move-group-grasp grasp --object $obj
        python3 ros-move-group-grasp release --object train_1
        python3 ros-move-group-grasp grasp --object train_1
        python3 ros-move-group-grasp grasp --object seed_pose_object
        
        echo "释放 $obj..."
        python3 ros-move-group-grasp release --object $obj
        python3 ros-move-group-grasp release --object train_1
        
        sleep 2
    done
done
EOF

chmod +x train_loop.sh
./train_loop.sh
步骤4：监控学习进度
bash
# 另开一个终端，实时查看数据
# 使用正确的路径
watch -n 2 'ls -la ~/qingfu_moveit/moveit_plugins/kinematics_plugins/ml_seed_predictor/src/data/'

watch -n 2 'ls -la ~/qingfu_moveit/moveit_plugins/kinematics_plugins/ml_seed_predictor/src/data/ && echo "" && cat ~/qingfu_moveit/moveit_plugins/kinematics_plugins/ml_seed_predictor/src/data/training_data.json 2>/dev/null | grep -o "\"count\": [0-9]*"'
步骤5：手动触发训练（可选）
bash
# 创建训练脚本
cat > ~/qingfu_moveit/train_ml.py << 'EOF'
#!/usr/bin/env python3
import sys
sys.path.insert(0, '/home/diyuanqiongyu/qingfu_moveit')

from moveit_bootstrap import init_moveit_paths
init_moveit_paths()

from kin_ik.ik_solver import IKSolver

ik = IKSolver()
if hasattr(ik, 'train_ml_model'):
    success = ik.train_ml_model()
    print(f"训练结果: {'✅成功' if success else '❌失败，需要更多数据'}")
else:
    print("❌ 没有 train_ml_model 方法")
EOF

chmod +x ~/qingfu_moveit/train_ml.py
python3 ~/qingfu_moveit/train_ml.py
步骤6：验证训练结果
bash
# 查看数据文件
ls -la ~/qingfu_moveit/moveit_plugins/kinematics_plugins/ml_seed_predictor/data/

# 应该看到：
# - training_data.json (样本数据)
# - model.pkl (训练好的模型)
步骤7：测试ML预测
bash
# 再抓取时，应该看到ML预测
python3 ros-move-group-grasp grasp --object train_1
# 日志中应有: [ML] ✅ 预测种子: ...
开始训练吧！抓取3轮（共15次）后应该能看到模型训练！ 🚀