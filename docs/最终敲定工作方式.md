## 最终敲定1
cd ~/qingfu_moveit
# 启动
./start_robotpress.sh

# 停止（新开终端）
./stop_robotpress.sh

# 1. 先编译接口包（.srv 文件）
cd ~/qingfu_moveit
colcon build --packages-select robot_interfaces
source install/setup.bash

# 2. 再编译服务包（依赖新接口）
colcon build --packages-select robot_services
source install/setup.bash

# 3. 重启服务
./stop_robotpress.sh
./start_robotpress.sh
